<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.powerassistant">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.PowerAssistant">
        <activity android:name=".tarrif"></activity>
        <activity android:name=".history" />
        <activity android:name=".deleteuser" />
        <activity android:name=".reset" />
        <activity android:name=".profile" />
        <activity android:name=".Settings" />
        <activity android:name=".MeterSettings" />
        <activity android:name=".Dashboard" />
        <activity android:name=".register" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>